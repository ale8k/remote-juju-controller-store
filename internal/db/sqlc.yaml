version: "2"
sql:
  - engine: "sqlite"
    schema:
      - "sql/schema.sql"
    queries:
      - "sql/queries/*.sql"
    gen:
      go:
        package: "sqlc"
        out: "sqlc"
        emit_json_tags: true
        emit_interface: true
        emit_empty_slices: true
