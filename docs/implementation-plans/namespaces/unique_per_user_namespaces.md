# Unique Per User Namespaces

Currently namespace names are globally unique. They should be unique to only the tenant (account). That is:

alex@example.com can have a namespace "default".
And so can:
bob@example.com can have a namespace "default".
